import pepita.*
import comidas.*
import masAves.*
import roque.*

test "roque trabaja" {
	
	roque.tuPupiloEs(pepita)
    roque.entrenar()
    roque.tuPupiloEs(pipa)
    roque.entrenar()
	assert.equals(pepita.energia(),1165)
	assert.equals(pepon,1178)
	

}